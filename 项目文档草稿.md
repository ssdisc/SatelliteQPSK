# 项目文档

## 1. 数据文件介绍

### 数据来源与背景

本项目所使用的数据源于一次为庆祝北京邮电大学建校70周年而特别设计的工程实践活动。该活动由“通信系统建模与仿真”课程团队与株洲太空星际公司合作，通过在轨的SAR（合成孔径雷达）遥感卫星对北京邮电大学西土城校区进行精确成像，并将原始回波数据实时传回地面。本教学案例的数据集即是这次实验的直接产出。

### 数据集详情

- **完整数据集**: 总大小为53.7GB的真实SAR卫星中频IQ数据。
- **数据格式**: `int16`格式的复数IQ数据，其中实部和虚部交替存储。
- **采样率**: 500 MHz。
- **文件名示例**: `sample_0611_500MHz_middle.bin`。

### 测试数据集

为了方便用户快速进行算法验证和系统调试，项目额外提供了一个小段测试数据文件。

- **文件名**: `small_sample_256k.bin`。
- **大小**: 1MB，包含256,000个复数点。
- **数据来源**: 从完整的53.7GB数据文件中提取的中间256K个数据点。
- **主要用途**: 用于快速验证算法的正确性、调试系统参数以及测试代码的基本功能，从而提高开发效率。

### 数据处理说明

数据文件记录的是由专业的地面站设备接收的信号。信号首先由高增益的相控阵天线接收，然后通过NI Ettus USRP X410软件定义无线电（SDR）设备进行处理，将原始的X波段下行信号下变频至1.2GHz的中频，并最终保存为`int16`格式的数字基带信号（复包络形式）。用户的任务便是对这份数字化的复包络信号进行后续的信号处理与解调。

## 2. 解调方法

### 总体技术指标

- **调制方式**: QPSK (四相相移键控)。
- **数据传输速率**: 150 Mbps，对应的符号率为 75 MBaud/s。
- **脉冲成形**: 采用滚降系数 α = 0.33 的根升余弦（RRC）滤波器。
- **帧结构**: 遵循CCSDS（空间数据系统咨询委员会）的AOS（先进轨道系统）标准，每帧长度为1024字节。
- **同步字**: 采用32比特的 `0x1ACFFC1D` 作为帧同步字，该同步字具有优秀的自相关特性。

### 解调流程与核心算法

整个解调过程是一个完整的数字接收机实现链路，包含以下核心模块：

1.  **信号预处理**:
    - **数据加载与重采样**: 从文件中加载原始500MHz采样率的IQ数据。为满足后续Gardner定时同步算法对每个符号至少2个采样点（sps=2）的要求，并兼顾计算效率，原始信号被重采样至150MHz。

2.  **信号整形**:
    - **RRC匹配滤波**: 使用与发射端匹配的RRC滤波器，以最大化信噪比并消除码间串扰。
    - **AGC (自动增益控制)**: 对信号幅度进行归一化，以适应后续判决模块的输入要求。

3.  **同步**:
    - **定时同步**: 采用非数据辅助的 **Gardner算法** 来恢复正确的符号采样时钟。在具体实现中，采用了三阶Farrow立方插值器，相比传统线性插值能提供更精确的分数延迟插值，在高符号率系统中优势明显。
    - **载波同步**: 采用判决辅助的二阶 **锁相环（PLL）** 或Costas环来估计和补偿信号中的载波频率和相位偏移。

4.  **数据恢复**:
    - **帧同步与相位模糊恢复**: 通过在数据流中搜索已知的32比特同步字 `0x1ACFFC1D` 来确定每一帧的起始位置。在此过程中，通过对接收信号进行四种可能的相位校正（0°, 90°, 180°, 270°）并选择相关峰值最大的一路，联合解决了QPSK解调后固有的相位模糊问题。
    - **解扰与验证**: 对帧同步后的数据进行解扰，该过程基于本原多项式 `1+X^14+X^15` 实现。为解决相位恢复后可能存在的IQ两路交换问题，通过检查帧末尾的两个比特进行验证。根据CCSDS关于LDPC系统码的编码规范，这两个比特恒为`00`。通过此方法可判断解扰的有效性并自动校正IQ次序。
    - **信道解码**: 信号经过了LDPC 7/8编码。由于采用的是LDPC系统码，信息位在编码过程中保持不变，校验位附加在信息位之后。考虑到LDPC软解码的计算复杂度极高，在实际处理中并未执行完整的解码步骤，而是直接截取帧中的信息位部分（AOS帧头及有效数据负载）进行后续处理。

### 地面数据处理流程

根据星上数据的加工流程（AOS格式编排 -> LDPC编码 -> 加扰），地面解调采用其逆流程：

**帧同步 -> 解扰 -> LDPC解码 -> 数据输出**

## 3. 卫星介绍

### 卫星信息

- **所有者/开发者**: 株洲太空星际公司。
- **类型**: 一颗自主研发的低轨道（LEO）SAR（合成孔径雷达）成像卫星。

### 技术参数

- **成像频段**: 9300 - 9900 MHz (X波段)。
- **数据下传频段**: 8025 - 8400 MHz (X波段)。

### 地面接收设备

- **天线系统**: 采用高增益的相控阵天线，能够自动跟踪卫星轨迹，确保在卫星过境期间稳定接收下行信号。
- **核心接收设备**: 使用 **NI Ettus USRP X410** 作为软件定义无线电（SDR）平台。它负责对下行信号进行处理，包括将其下变频至1.2 GHz的中频，并进行数字化采样和录制。